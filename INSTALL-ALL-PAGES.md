# Инструкция по установке виджета на все страницы nm-shop.by

## 📋 Важно перед началом

Виджет уже протестирован и работает на тестовой странице: `https://nm-shop.by/kuhonnaya-mebel/kuhnya-variant-fasada-kvadro/`

**Эта инструкция для установки виджета на ВСЕ страницы сайта.**

---

## 🔧 Способ 1: Через FTP (Рекомендуется)

### Шаг 1: Найдите файл header.twig

**Путь к файлу:**
```
catalog/view/theme/[НАЗВАНИЕ_ТЕМЫ]/template/common/header.twig
```

**Как найти название темы:**

**Вариант А - Через админ-панель:**
1. Войдите в админ-панель OpenCart: `https://nm-shop.by/admin/`
2. Перейдите: **System → Settings**
3. Выберите ваш магазин
4. Вкладка **Store**
5. Найдите поле **Theme** — это название темы

**Вариант Б - Через FTP:**
- Обычные названия тем: `default`, `journal3`, `fastor`, `themeXXX`
- Проверьте папку `catalog/view/theme/` — там будет папка с названием темы

---

### Шаг 2: Откройте файл для редактирования

**Через FTP/SFTP:**
1. Подключитесь к серверу nm-shop.by
2. Перейдите в: `catalog/view/theme/[ТЕМА]/template/common/`
3. Откройте файл `header.twig` в текстовом редакторе
4. **Важно:** Сделайте бэкап файла перед редактированием (скачайте на компьютер)

**Или через админку (если есть Theme Editor):**
1. Войдите в админ-панель
2. Перейдите: **Design → Theme Editor**
3. Выберите текущую тему
4. Найдите файл `common/header.twig`

---

### Шаг 3: Найдите строку `</head>`

Откройте `header.twig` и найдите закрывающий тег `</head>`. 

Обычно он в конце файла, перед `</html>` или перед началом `<body>`.

**Пример структуры файла:**
```html
<head>
  <meta charset="utf-8">
  <title>...</title>
  ...
  <!-- ВСТАВЬТЕ КОД ЗДЕСЬ, ПЕРЕД ЭТОЙ СТРОКОЙ -->
</head>
```

**Если в файле уже есть код виджета для тестовой страницы** — найдите его и замените на новый код из `CLIENT-INSTALL-CODE-FULL.txt`.

---

### Шаг 4: Вставьте код перед `</head>`

1. Откройте файл `CLIENT-INSTALL-CODE-FULL.txt`
2. Скопируйте **весь код** из этого файла (от `<!--` до `</style>` включительно)
3. Вставьте его **перед** строкой `</head>` в `header.twig`

**Важно:**
- ✅ Код должен быть **перед** `</head>`, не после
- ✅ Не удаляйте существующий код, только добавьте новый
- ✅ Сохраните файл в кодировке **UTF-8**
- ✅ Если был тестовый код — замените его полностью на новый

**Пример правильной вставки:**
```html
<head>
  <meta charset="utf-8">
  <title>...</title>
  ...
  
  <!-- КОД ВИДЖЕТА ВСТАВЛЯЕТСЯ ЗДЕСЬ -->
  <link rel="preconnect" href="https://widget-nine-murex.vercel.app" crossorigin>
  <script>...</script>
  <style>...</style>
  
</head>
```

---

### Шаг 5: Очистите кеш OpenCart

**ОБЯЗАТЕЛЬНО!** Без очистки кеша виджет не появится.

**Через админ-панель:**

1. **Extensions → Modifications**
   - Нажмите кнопку **Refresh** (синяя иконка обновления вверху)
   - Дождитесь завершения (может занять 1-2 минуты)

2. **Dashboard → Clear cache**
   - Нажмите на иконку **шестеренки** справа вверху
   - Выберите **Clear cache**

**Или через FTP:**
1. Удалите папку `system/storage/cache/` через FTP
2. Создайте пустую папку `cache` заново
3. Убедитесь, что права доступа правильные (обычно 755)

---

### Шаг 6: Проверьте на всех страницах

**Обязательная проверка:**

1. **Главная страница:**
   - Откройте: `https://nm-shop.by/`
   - Виджет должен появиться в правом нижнем углу через 60-90 секунд

2. **Страница каталога:**
   - Откройте любую категорию, например: `https://nm-shop.by/kuhonnaya-mebel/`
   - Виджет должен появиться

3. **Страница товара:**
   - Откройте любой товар
   - Виджет должен появиться

4. **Другие страницы:**
   - Контакты, О нас, и т.д.
   - Виджет должен работать везде

5. **Мобильная версия:**
   - Откройте сайт на смартфоне
   - Виджет не должен перекрывать нижнее меню
   - Кнопка должна быть видна и доступна

6. **Консоль браузера (F12 → Console):**
   - Не должно быть красных ошибок
   - Могут быть желтые предупреждения — это нормально

---

## 🔧 Способ 2: Через модуль "Custom HTML" или "HTML Content"

Если нет доступа к FTP или удобнее через админ-панель.

### Шаг 1: Найдите модуль

1. Войдите в админ-панель OpenCart: `https://nm-shop.by/admin/`
2. Перейдите: **Extensions → Extensions**
3. Выберите тип расширения: **Modules**
4. Найдите один из модулей:
   - **Custom HTML**
   - **HTML Content**
   - **Custom Code**
   - **HTML Module**

---

### Шаг 2: Установите модуль (если еще не установлен)

1. Если модуль не найден, перейдите: **Extensions → Installer**
2. Загрузите модуль Custom HTML (если есть в вашем распоряжении)
3. Или используйте встроенные возможности OpenCart

---

### Шаг 3: Настройте модуль

1. В списке модулей найдите **Custom HTML** (или аналог)
2. Нажмите **Edit** (карандаш) или **Install** (если еще не установлен)
3. Настройки:
   - **Status**: **Enabled** (Включено)
   - **Position**: **Header** (или "Before </head>")
   - **Sort Order**: **1** (или минимальное значение)
   - **Layout**: **All Layouts** (или выберите все нужные)
4. В поле **HTML** или **Content** вставьте код из `CLIENT-INSTALL-CODE-FULL.txt`
5. Нажмите **Save**

**Важно:** Если уже есть модуль с тестовым кодом — отредактируйте его и замените код на новый.

---

### Шаг 4: Очистите кеш

1. **Extensions → Modifications → Refresh**
2. **Dashboard → Clear cache**

---

### Шаг 5: Проверьте

Проведите такую же проверку, как в Шаге 6 Способа 1.

---

## ✅ Чеклист проверки работоспособности

### Функциональное тестирование:

- [ ] **Виджет появляется на главной странице**
  - Откройте `https://nm-shop.by/` в режиме инкогнито
  - Подождите 60-90 секунд
  - Виджет должен появиться в правом нижнем углу

- [ ] **Виджет появляется на страницах каталога**
  - Откройте любую категорию
  - Виджет должен работать

- [ ] **Виджет появляется на страницах товаров**
  - Откройте любой товар
  - Виджет должен работать

- [ ] **Виджет появляется на других страницах** (контакты, о нас, и т.д.)

- [ ] **Чат работает**
  - Нажмите на виджет
  - Отправьте сообщение: "Какие у вас диваны?"
  - Бот должен ответить через 2-5 секунд

- [ ] **Форма подарка работает**
  - Выберите категорию (например: "Диван")
  - Выберите подарок
  - Заполните форму (имя, телефон, мессенджер)
  - Отправьте форму
  - Проверьте Google Sheets — должна появиться запись

- [ ] **Мобильная версия работает**
  - Откройте сайт на смартфоне
  - Виджет не перекрывает нижнее меню
  - Кнопка доступна для нажатия
  - Чат открывается на весь экран

### Проверка консоли:

- [ ] **Нет ошибок в Console (F12 → Console)**
  - Не должно быть красных ошибок
  - Могут быть желтые предупреждения — это нормально

- [ ] **Виджет загружается правильно**
  - В Network (F12 → Network) должна быть загрузка `widget.js`
  - Статус должен быть 200 OK

### Проверка админ-панели виджета:

- [ ] **Sessions появляются в админке**
  - Откройте: `https://widget-nine-murex.vercel.app/admin.html`
  - Войдите с паролем
  - Переключите источник на **nm-shop**
  - Должны появляться новые сессии с правильным source

- [ ] **Аналитика работает**
  - В разделе "Аналитика" должны быть данные о просмотрах
  - В разделе "Ошибки" не должно быть критических ошибок

### Проверка визуального:

- [ ] **Виджет не перекрывает важные элементы**
  - Корзина доступна
  - Меню работает
  - Кнопки не закрыты виджетом

- [ ] **Виджет не дублируется**
  - Должна быть только одна кнопка виджета
  - Нет дублирования кода в header.twig

---

## 🐛 Решение проблем

### Проблема 1: Виджет не появляется

**Решения:**
- ✅ Убедитесь, что кеш очищен: **Extensions → Modifications → Refresh** + **Clear cache**
- ✅ Проверьте путь к header.twig (правильная тема?)
- ✅ Откройте Console (F12) — есть ошибки загрузки?
- ✅ Убедитесь, что код вставлен **ПЕРЕД** `</head>`, а не после
- ✅ Проверьте, что файл сохранен в правильной кодировке (UTF-8)
- ✅ Проверьте, что код виджета не заблокирован AdBlock или другими расширениями

---

### Проблема 2: Виджет появляется дважды

**Причина:** Код вставлен в несколько мест

**Решение:**
- Проверьте `header.twig`, `footer.twig`
- Проверьте активные модули "Custom HTML"
- Удалите дублирующий код
- Код уже содержит защиту от двойной загрузки (`window.nmShopWidgetLoaded`)

---

### Проблема 3: Виджет перекрывается другими элементами

**Решение:**
- Код интеграции уже содержит `z-index: 9999999 !important`
- Если проблема сохраняется, проверьте стили других элементов сайта
- На мобильных: кнопка поднята выше (`bottom: 80px` на экранах < 768px)

---

### Проблема 4: Не работает после обновления темы

**Причина:** OpenCart затер изменения при обновлении

**Решение:**
- Пересоздайте модуль Custom HTML
- Или используйте OCMOD (модификацию) для автоматической вставки
- Сделайте бэкап настроенного кода

---

### Проблема 5: Не сохраняются лиды в Google Sheets

**Решения:**
- Проверьте GAS endpoint (откройте в браузере — должен вернуть "OK")
- Проверьте CORS (должен быть разрешен для nm-shop.by)
- Проверьте права доступа к Google Sheets
- Откройте Console (F12) — есть ошибки при отправке формы?

---

### Проблема 6: Виджет работает только на некоторых страницах

**Причина:** Возможно, код вставлен не в общий header.twig

**Решение:**
- Убедитесь, что редактируете `common/header.twig` (это общий файл для всех страниц)
- Не редактируйте header.twig для конкретных страниц/категорий

---

## 📊 Мониторинг после установки

### Где смотреть лиды:

- **Google Sheets**: https://docs.google.com/spreadsheets/d/1wYQdnQ0gAMeWXnrKOS74UTsxjmE4z6QYRybI3TcC2TU/
- Каждый лид содержит: дату, телефон, подарок, URL страницы, SessionID, имя, категорию, мессенджер, пожелания

### Админ-панель виджета:

- **URL**: https://widget-nine-murex.vercel.app/admin.html
- **Пароль**: (уточните у владельца)
- Показывает:
  - Sessions (диалоги с пользователями)
  - Analytics (статистика посещений, открытий, форм)
  - Errors (ошибки и медленные запросы)

**Важно:** После установки переключите источник на **nm-shop** в админ-панели, чтобы видеть статистику только с сайта клиента.

### Health Check:

Откройте в браузере:
```
https://widget-nine-murex.vercel.app/api/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "services": {
    "redis": "connected",
    "openai": "available"
  }
}
```

---

## 🔄 Обновление виджета в будущем

Если нужно обновить виджет:

1. Измените файлы в GitHub репозитории
2. Commit → push
3. Vercel автоматически обновит за 2-3 минуты
4. Обновите версию в коде интеграции: `?v=5.1.1` (или следующая версия)
5. Очистите кеш OpenCart

---

## ✅ Финальный чеклист

- [ ] Код из `CLIENT-INSTALL-CODE-FULL.txt` вставлен в `header.twig` перед `</head>`
- [ ] Если был тестовый код — он заменен на новый
- [ ] Кеш OpenCart очищен (Modifications + Cache)
- [ ] Виджет появляется на главной странице
- [ ] Виджет появляется на страницах каталога
- [ ] Виджет появляется на страницах товаров
- [ ] Виджет появляется на других страницах (контакты, о нас)
- [ ] Нет ошибок в Console (F12)
- [ ] Чат работает, OpenAI отвечает
- [ ] Форма отправляется в Google Sheets
- [ ] Лид появился в таблице с URL страницы
- [ ] Протестирована мобильная версия
- [ ] Нет конфликтов с другими элементами OpenCart
- [ ] Sessions появляются в админке с source "nm-shop"
- [ ] Виджет не дублируется

---

**Готово!** Виджет должен работать стабильно на всех страницах nm-shop.by 🎉

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Health Check API
3. Проверьте логи Vercel (vercel.com → проект → Logs)
4. Убедитесь, что кеш OpenCart очищен
5. Проверьте, что код вставлен правильно (перед `</head>`)

