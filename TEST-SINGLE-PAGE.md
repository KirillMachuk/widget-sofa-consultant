# 🧪 Тестирование виджета на одной странице

## Зачем тестировать на одной странице?

- ✅ Безопасно проверить работу виджета
- ✅ Убедиться, что нет конфликтов с дизайном
- ✅ Протестировать функционал перед массовым внедрением
- ✅ Легко откатить изменения, если что-то не так

## 📋 Пошаговая инструкция

### Шаг 1: Выберите страницу для тестирования

Рекомендуемые варианты:
- **Главная страница** (`/` или `/index.php?route=common/home`) - проще всего
- **Страница товара** - проверить работу на странице с формой заказа
- **Страница категории** - проверить работу в каталоге

### Шаг 2: Определите URL страницы

1. Откройте нужную страницу на nm-shop.by
2. Посмотрите в адресной строке браузера
3. Скопируйте путь (без домена)

**Примеры URL:**
```
Главная: /
Или: /index.php?route=common/home

Товар: /index.php?route=product/product&product_id=123
Или: /product/диван-книжка-divano-volna

Категория: /index.php?route=product/category&path=20
Или: /catalog/myagkaya-mebel
```

### Шаг 3: Откройте header.twig

1. Подключитесь к серверу через FTP
2. Перейдите в: `catalog/view/theme/[ТЕМА]/template/common/`
3. Откройте файл `header.twig` для редактирования
4. **Создайте бэкап** (скачайте файл на компьютер)

### Шаг 4: Вставьте тестовый код

1. Откройте файл `opencart-integration-test-page.txt`
2. Найдите строку:
   ```javascript
   const TEST_PAGE_URL = '/'; // ← ЗАМЕНИТЕ на URL вашей тестовой страницы
   ```
3. Замените `'/'` на URL вашей тестовой страницы

**Пример для главной:**
```javascript
const TEST_PAGE_URL = '/';
```

**Пример для товара:**
```javascript
const TEST_PAGE_URL = '/index.php?route=product/product&product_id=123';
```

4. Скопируйте весь код из `opencart-integration-test-page.txt`
5. Вставьте в `header.twig` **перед** `</head>`
6. Сохраните файл и загрузите обратно на сервер

### Шаг 5: Очистите кеш OpenCart

1. Войдите в админ-панель: `https://nm-shop.by/admin/`
2. Перейдите: **Extensions → Modifications**
3. Нажмите **Refresh** (синяя кнопка)
4. Перейдите в **Dashboard**
5. Нажмите на **шестеренку** → **Clear cache**

### Шаг 6: Проверьте работу

1. Откройте тестовую страницу в режиме инкогнито
2. Просмотрите страницу (прокрутите вниз)
3. Подождите 60-90 секунд
4. В правом нижнем углу должен появиться виджет
5. Откройте консоль браузера (F12 → Console)
   - Не должно быть ошибок (красные сообщения)
   - Может быть сообщение: `Chat widget: загрузка на тестовой странице: /`

### Шаг 7: Протестируйте функционал

1. **Откройте чат** - нажмите на виджет
2. **Отправьте сообщение** - например: "Какие у вас диваны?"
3. **Проверьте ответ бота** - должен ответить через 2-5 секунд
4. **Заполните форму подарка:**
   - Выберите категорию (например: "Диван")
   - Выберите подарок
   - Заполните имя, телефон, мессенджер
   - Отправьте форму
5. **Проверьте Google Sheets:**
   - Откройте: https://docs.google.com/spreadsheets/d/1wYQdnQ0gAMeWXnrKOS74UTsxjmE4z6QYRybI3TcC2TU/
   - Должна появиться новая строка с вашими данными

### Шаг 8: Проверьте другие страницы

1. Откройте другие страницы сайта
2. Виджет **НЕ должен** появляться на них
3. В консоли может быть сообщение: `Chat widget: тестовая страница не совпадает, виджет не загружается`

## ✅ Если все работает

После успешного тестирования:

1. Откройте `header.twig`
2. Замените тестовый код на полную версию из `opencart-integration-simple.txt`
3. Удалите проверку `TEST_PAGE_URL` (или закомментируйте её)
4. Сохраните файл
5. Очистите кеш OpenCart

**Или** просто замените код на полную версию:

```javascript
// Удалите эти строки:
const TEST_PAGE_URL = '/';
const currentPath = window.location.pathname + window.location.search;
const isTestPage = currentPath === TEST_PAGE_URL || 
                   currentPath.startsWith(TEST_PAGE_URL) ||
                   window.location.href.includes(TEST_PAGE_URL);
if (!isTestPage) {
  console.log('Chat widget: тестовая страница не совпадает, виджет не загружается');
  return;
}
```

## ❌ Если что-то не работает

### Виджет не появляется на тестовой странице

**Проверьте:**
1. Правильно ли указан `TEST_PAGE_URL`?
2. Откройте консоль (F12) - есть ли ошибки?
3. Есть ли сообщение о загрузке виджета?
4. Очистили ли кеш OpenCart?

**Решение:**
- Проверьте URL страницы еще раз (может быть `/index.php?route=common/home` вместо `/`)
- Попробуйте использовать только путь без параметров: `const TEST_PAGE_URL = '/';`

### Виджет появляется на всех страницах

**Причина:** Проверка URL не работает

**Решение:**
- Убедитесь, что код проверки не закомментирован
- Проверьте, что `TEST_PAGE_URL` указан правильно
- Добавьте более строгую проверку:

```javascript
const currentPath = window.location.pathname + window.location.search;
if (currentPath !== TEST_PAGE_URL && !currentPath.startsWith(TEST_PAGE_URL + '&')) {
  return;
}
```

### Ошибки в консоли

**Проверьте:**
- Откройте Console (F12)
- Есть ли красные ошибки?
- Какие именно ошибки?

**Типичные проблемы:**
- CORS ошибки - проверьте, что виджет загружается с правильного домена
- 404 ошибки - проверьте URL виджета
- 405 ошибки на `/api/chat` - это нормально (API принимает только POST)

## 🔄 Быстрое переключение между тестом и production

Если хотите быстро переключаться между тестовой и полной версией:

1. Создайте два файла:
   - `header.twig.test` - с тестовым кодом
   - `header.twig.prod` - с полным кодом

2. При тестировании: переименуйте `header.twig.test` → `header.twig`
3. Для production: переименуйте `header.twig.prod` → `header.twig`

## 📝 Чеклист тестирования

- [ ] Выбрана страница для тестирования
- [ ] Определен URL страницы
- [ ] Создан бэкап header.twig
- [ ] Вставлен тестовый код с правильным TEST_PAGE_URL
- [ ] Кеш OpenCart очищен
- [ ] Виджет появляется на тестовой странице
- [ ] Виджет НЕ появляется на других страницах
- [ ] Чат работает, бот отвечает
- [ ] Форма отправляется в Google Sheets
- [ ] Лид появился в таблице
- [ ] Нет ошибок в консоли
- [ ] Протестирована мобильная версия

---

**Готово!** После успешного тестирования замените код на полную версию. 🎉

